import { store as funnel } from '../src/funnel'
import { store as variables } from '../src/variables'
import { store as step } from '../src/step'
import { store as action } from '../src/action'
import { store as funnelShow } from '../src/funnelShow'
import { store as actionButton } from '../src/actionButton'
import { store as marketingAction } from '../src/marketingAction'
import { store as customersImport } from '../src/customersImport'

export default {
    funnel,
    variables,
    step,
    action,
    funnelShow,
    actionButton,
    marketingAction,
    customersImport
}
