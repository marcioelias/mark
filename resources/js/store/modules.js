import { store as funnel } from '../src/funnel'
import { store as variables } from '../src/variables'
import { store as steps } from '../src/steps'
import { store as sms_action } from '../src/sms_action'
import { store as email_action } from '../src/email_action'

export default {
    funnel,
    variables,
    steps,
    sms_action,
    email_action
}
