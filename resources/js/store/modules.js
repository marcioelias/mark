import { store as funnel } from '../src/funnel'
import { store as variables } from '../src/variables'
import { store as step } from '../src/step'
import { store as action } from '../src/action'

export default {
    funnel,
    variables,
    step,
    action
}
